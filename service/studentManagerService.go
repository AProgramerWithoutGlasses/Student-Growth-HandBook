package service

import (
	jsonvalue "github.com/Andrew-M-C/go.jsonvalue"
)

// 判断前端发来的结构体中非空字段的内容
func GetNotEmptyFieldMap(queryStuReq *jsonvalue.V) (map[string]any, map[string]any) {

	return nil, nil
}
